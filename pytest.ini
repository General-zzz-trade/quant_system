[pytest]
testpaths =
    tests
    tests_unit
    execution/tests
addopts = -q --tb=short -m "not benchmark"
markers =
    benchmark: performance benchmark tests (deselect with '-m "not benchmark"')
    slow: longer-running tests
