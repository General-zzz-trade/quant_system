{
  "dashboard": {
    "id": null,
    "uid": "quant-trading",
    "title": "Quant Trading System",
    "tags": ["trading", "quant"],
    "timezone": "utc",
    "refresh": "5s",
    "panels": [
      {
        "id": 1,
        "title": "PnL (Cumulative)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
        "targets": [
          {
            "expr": "quant_pnl_total",
            "legendFormat": "PnL"
          }
        ]
      },
      {
        "id": 2,
        "title": "Pipeline Latency P99 (ms)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
        "targets": [
          {
            "expr": "histogram_quantile(0.99, rate(quant_pipeline_latency_ms_bucket[5m]))",
            "legendFormat": "p99"
          },
          {
            "expr": "histogram_quantile(0.50, rate(quant_pipeline_latency_ms_bucket[5m]))",
            "legendFormat": "p50"
          }
        ]
      },
      {
        "id": 3,
        "title": "ML Score",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
        "targets": [
          {
            "expr": "quant_ml_score",
            "legendFormat": "{{symbol}}"
          }
        ]
      },
      {
        "id": 4,
        "title": "Inference Latency (ms)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
        "targets": [
          {
            "expr": "histogram_quantile(0.99, rate(quant_inference_latency_ms_bucket[5m]))",
            "legendFormat": "p99"
          },
          {
            "expr": "histogram_quantile(0.50, rate(quant_inference_latency_ms_bucket[5m]))",
            "legendFormat": "p50"
          }
        ]
      },
      {
        "id": 5,
        "title": "WebSocket Connection State",
        "type": "stat",
        "gridPos": {"h": 4, "w": 6, "x": 0, "y": 16},
        "targets": [
          {
            "expr": "quant_ws_connection_state",
            "legendFormat": "{{state}}"
          }
        ]
      },
      {
        "id": 6,
        "title": "Order Rejections",
        "type": "stat",
        "gridPos": {"h": 4, "w": 6, "x": 6, "y": 16},
        "targets": [
          {
            "expr": "increase(quant_orders_rejected_total[5m])",
            "legendFormat": "rejections"
          }
        ]
      },
      {
        "id": 7,
        "title": "Position Size (Notional)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 20},
        "targets": [
          {
            "expr": "quant_position_notional",
            "legendFormat": "{{symbol}}"
          }
        ]
      },
      {
        "id": 8,
        "title": "Market Data Age (sec)",
        "type": "timeseries",
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 20},
        "targets": [
          {
            "expr": "quant_market_data_age_sec",
            "legendFormat": "data_age"
          }
        ]
      }
    ]
  }
}
