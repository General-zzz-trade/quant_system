# Status: candidate-production Kubernetes manifest.
# This is not the current default release path.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quant-engine-data
  namespace: quant
  labels:
    app: quant-engine
    component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
