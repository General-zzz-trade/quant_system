# Deprecated: use the repo-root Dockerfile target `paper`.
# This file is intentionally kept only to fail fast for stale automation.
FROM python:3.12-slim

RUN /bin/sh -lc 'echo "deploy/docker/Dockerfile is deprecated. Use the repo-root Dockerfile target paper." >&2; exit 1'
